
<!-- ai-dev-flow-server v3.2 -->
## AI Dev Flow v3.2

本项目已安装 ai-dev-flow-server 开发流程约束。

### 核心命令

| 命令 | 用途 | 默认入口 |
|------|------|:---:|
| `/grill-with-docs` | 基于文档对话澄清需求 | ⬅ 默认 |
| `/to-spec` | 需求 → 规格（可逆向） | grill 之后 |
| `/to-tickets` | 规格 → 工单拆分 | spec 评审之后 |
| `/tdd` | 按 AC 写失败测试 + 接口 stub | ticket 就绪后 |
| `/implement` | 工单 → 代码（自动重试最多 3 次） | TDD RED 🔴 之后 |
| `/code-review` | 独立子代理审查 diff（每批次 + PR 前） | 同层 GREEN 后 / PR 前 |
| `/review-cc-cli` | 独立会话评审（代码/文档/方案） | spec 大型任务 / 代码合入前 |

### 完整路径

```
grill → to-spec → [spec 评审] → to-tickets → /tdd → [C1-C5] → /implement(自动重试) → /code-review(批次)
                       ↑                          ↑                     ↑                ↑
              大型: /review-cc-cli          唯一人工确认点          全自动        同层 GREEN 后
              中型: 自查 S1-S10          确认 RED 为真                       + PR 前最终审查
              简单: 跳过
```

### 阶段状态机

```
explore → spec → tickets → tdd → implement → done
   ↑ 自动追踪，hook 驱动，无需手动操作
```

### Ticket 状态机

```
backlog → ready → in_progress → done
```

- 人将 ticket 拖到 `ready` → dispatch.timer 自动消化
- dispatch.sh: 宪法检查 → 原子抢占 → Archon 工作流 → PR
- PR 创建后 Telegram 通知审批

### 关键路径

| 内容 | 路径 |
|------|------|
| 项目配置 | `.devflow/config.yaml` |
| 知识库 | `.devflow/knowledge/` |
| 阶段状态 | `.devflow/stage` |
| Ticket 目录 | `issues/` |
| 调度日志 | `logs/dispatch.log` |

### 安全红线

含 `auth`/`payment`/`crypto`/`delete`/`permission` 标记的 PR **禁止自动合并**，必须人工逐行审查。

### 计划文件管理（防覆盖）

- 每次新计划创建新文件，文件名含日期+主题，禁止覆盖已有计划文件
- 计划执行完毕后，关键设计决策（权限边界、接口约束、架构取舍、被拒绝的方案）必须提取为 ADR
- ADR 存放：项目有 `docs/decisions/` 则写项目，否则写 `~/.claude/plans/decisions/`
- 旧计划文件保留不删；计划只存执行步骤，不可变决策回流正式文档

### ADR 格式

```markdown
# ADR-NNN: <标题>
## 状态：已采纳 / 已废弃
## 日期：YYYY-MM-DD
## 背景
## 决策
## 后果
## 拒绝的方案
```
