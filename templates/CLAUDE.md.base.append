
<!-- ai-dev-flow-server v3.0 -->
## AI Dev Flow v3.0

本项目已安装 ai-dev-flow-server 开发流程约束。

### 五大命令

| 命令 | 用途 | 默认入口 |
|------|------|:---:|
| `/grill-with-docs` | 基于文档对话澄清需求 | ⬅ 默认 |
| `/to-spec` | 需求 → 规格（可逆向） | grill 之后 |
| `/to-tickets` | 规格 → 工单拆分 | spec 之后 |
| `/implement` | 工单 → 代码 + 内建审查 | ticket 就绪后 |
| `/code-review` | 独立子代理审查 diff | implement 之后 |

### 阶段状态机

```
explore → spec → tickets → implement → done
```

### Ticket 状态机

```
backlog → ready → in_progress → done
```

- 人将 ticket 拖到 `ready` → dispatch.timer 自动消化
- dispatch.sh: 宪法检查 → 原子抢占 → Archon 工作流 → PR
- PR 创建后 Telegram 通知审批

### 关键路径

| 内容 | 路径 |
|------|------|
| 项目配置 | `.devflow/config.yaml` |
| 知识库 | `.devflow/knowledge/` |
| 阶段状态 | `.devflow/stage` |
| Ticket 目录 | `issues/` |
| 调度日志 | `logs/dispatch.log` |

### 安全红线

含 `auth`/`payment`/`crypto`/`delete`/`permission` 标记的 PR **禁止自动合并**，必须人工逐行审查。

### 计划文件管理（防覆盖）

- 每次新计划创建新文件，文件名含日期+主题，禁止覆盖已有计划文件
- 计划执行完毕后，关键设计决策（权限边界、接口约束、架构取舍、被拒绝的方案）必须提取为 ADR
- ADR 存放：项目有 `docs/decisions/` 则写项目，否则写 `~/.claude/plans/decisions/`
- 旧计划文件保留不删；计划只存执行步骤，不可变决策回流正式文档

### ADR 格式

```markdown
# ADR-NNN: <标题>
## 状态：已采纳 / 已废弃
## 日期：YYYY-MM-DD
## 背景
## 决策
## 后果
## 拒绝的方案
```
