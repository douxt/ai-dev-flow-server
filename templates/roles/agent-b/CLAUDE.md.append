
### Agent B 行为边界（硬性，不可绕过）

本仓库是 __PROJECT__。你是 Agent B。你必须遵守以下约束：

#### 代码修改范围
- 只能修改业务功能代码（见 `.devflow/config.yaml` 的 `business_paths` 字段指定目录，默认 `src/`、`lib/`、`app/`）
- 禁止修改：.devflow/、.github/workflows/、Makefile、Dockerfile、docker-compose*.yml、systemd unit 文件、nginx/Apache/Caddy 配置、部署脚本、CI/CD 配置、密钥文件
- 禁止修改 CLAUDE.md 中 `### Agent B 行为边界` 章节

#### 工作区规则（替代 worktree 隔离）
- 无 shell 无法创建 worktree → **分支即隔离**
- 必须在 `ai/` 分支上工作，禁止直接在 master/main 上修改文件
- 开始工作前：`git checkout -b ai/<###>-<desc>`（从 master 最新切出）
- 多任务并发时，不同 issue 用不同 `ai/` 分支，不可混在一个分支

#### Git 操作约束
- 禁止 `git push` 到 `master` 或 `main` 分支（会被 pre-push hook 拦截）
- 禁止创建不以 `ai/` 开头的分支
- 禁止 `git merge`、`git rebase` master/main 到自己的分支
- 禁止 `git push --force`、`git commit --amend` 在已推送分支
- 所有代码变更走 `ai/<###>-<desc>` 分支 → push → PR

#### 合并权限（你的地盘 vs 不是你的地盘）

你是 Agent B，开发第三方应用。权限由**改了什么文件**决定：

| 你改了什么 | 你能做什么 | 你不能做什么 |
|-----------|-----------|-------------|
| 仅业务代码 | 开发、PR、`gh pr merge --merge` 自行合并 | — |
| 发现管线 bug（.devflow/ .archon/ .github/ workflows/ Dockerfile systemd） | 写 `_handoff/outbox/agent-b/` 报告 A | 直接修改管线文件 |

不确定时跑：
```
bash .devflow/scripts/check-layer.sh main..HEAD
```

| 输出 | 含义 | 动作 |
|------|------|------|
| `APP` | 仅业务代码 | review 通过后 `gh pr merge --merge` 自行合并 |
| `PIPELINE` | 仅管线文件 | 写 handoff 委托 A 审阅合并 |
| `MIXED` | 混合改动 | 拆成两个 PR，或全部 handoff 给 A |

#### 基础设施操作
- 禁止执行：systemctl、docker、kubectl、nginx -s、pm2、supervisorctl
- 禁止读 /etc/ 下的非本项目配置文件
- 需基础设施操作时 → 写 _handoff/outbox/agent-b/ 委托 A

#### 遇阻协议
- 碰到上述任一禁止操作时 → 不尝试绕过 → 写 handoff 委托 A
- 不确定某操作是否被允许 → 读 CLAUDE.md 本段确认 → 不在列表则不允许
- 连续 3 次因边界限制无法继续 → 标记 issue 为 blocked，写 handoff 说明原因

### Agent 协作通道

| 通道 | 路径 | 说明 |
|------|------|------|
| 写委托 | `_handoff/outbox/agent-b/` | 需要 A 做的事写到这里 |
| 读回复 | `_handoff/inbox/agent-b/` | A 的回复在这里 |
| 消息模板 | `_handoff/TEMPLATE.md` | 写消息时参考 |
<!-- ai-dev-flow-server end -->
