# Gate 5: 脚本准备

## ⚠️ 安全规则（不可跳过）

1. 每完成一个步骤，输出结果给用户，**等待用户确认后才能继续下一步**
2. 禁止自动执行多个步骤，禁止跳过用户确认环节
3. 这不是 CC Workflow 脚本——不做后台子代理，全程与用户交互
4. 遇到不确定的判断，询问用户，不自行决策
5. 全程与用户交互，不 spawn 子代理

## 步骤 0：确认当前 Gate 进度

Read `.gate-state`，确认 gate-5 当前状态：
- 如果 `status: passed` → 跳过，告知用户
- 如果 `status: in_progress` 且有中断记录 → 从断点继续
- 确认 gate-4 必须为 `passed`，否则告知用户"请先完成 Gate 4"
- 如果 `status: pending` → 继续步骤 1

## 步骤 1：读脚本质量宪法

Read `05-脚本质量宪法.md`（如项目中不存在，Read `ai-dev-flow/05-脚本质量宪法.md`），理解 12 项脚本宪法规则。向用户概述关键规则。

**等待用户确认后继续。**

## 步骤 2：检查 AFK 脚本栈

Read `06-AFK脚本栈规范.md`，理解脚本清单和命名约定：
1. 检查 `prep-once.sh` 是否存在（项目 `.devflow/scripts/prep-once.sh`）
2. 如存在 → 继续步骤 3
3. 如不存在 → 告知用户需根据 06-AFK脚本栈规范自行创建 `prep-once.sh`

**等待用户确认后继续。**

## 步骤 3：脚本宪法检查

对 `prep-once.sh` 按 12 项脚本宪法逐条检查：

| # | 检查项 | 状态 |
|:-:|--------|:----:|
| 1 | 文件头含元信息签名 | |
| 2 | set -euo pipefail | |
| 3 | DRY_RUN 模式 | |
| 4 | 幂等保护 | |
| 5 | 错误处理（trap ERR） | |
| 6 | 日志函数（带时间戳） | |
| 7 | 预检（依赖检查） | |
| 8 | 输入验证 | |
| 9 | 输出清晰（✅/❌/⚠️） | |
| 10 | 无硬编码路径 | |
| 11 | 无密钥泄露 | |
| 12 | help 选项 | |

> 参照 `05-脚本质量宪法.md` 获取每项的详细检查方法。

**全部通过后**：更新 `.gate-state` gate-5 status → `passed`。
**不通过**：列出缺失项，用户修正脚本后重新检查。
